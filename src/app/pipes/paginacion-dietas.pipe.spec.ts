import { PaginacionDietasPipe } from './paginacion-dietas.pipe';

describe('PaginacionDietasPipe', () => {
  it('create an instance', () => {
    const pipe = new PaginacionDietasPipe();
    expect(pipe).toBeTruthy();
  });
});



import { PaginacionRutinasPipe } from './paginacion-rutinas.pipe';

describe('PaginacionRutinasPipe', () => {
  it('create an instance', () => {
    const pipe = new PaginacionRutinasPipe();
    expect(pipe).toBeTruthy();
  });
});
